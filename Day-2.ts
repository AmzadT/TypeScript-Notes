// npm install -g typescript (TypeScript Installation)
// tsc --v (version check)

var num:number = 10;
num = 20;
console.log(num);

// run command = tsc + fileName
// tsc --init (Configuration file)


function sum(a:number, b:number):number
{
    return a + b;
}

console.log(sum(10, 20));